import { ElementParams } from '../types';

const footerSection: ElementParams = {
  tag: 'footer',
  classNames: ['footer'],
};

const footerLogo: ElementParams = {
  tag: 'a',
  classNames: ['footer-logo'],
};

const footerLink: ElementParams = {
  tag: 'a',
  classNames: ['footer-link'],
};

const img: ElementParams = {
  tag: 'img',
  classNames: [''],
};

export {
  footerSection, footerLogo, footerLink, img,
};
